"use strict";
let arr = [1, 2];
let brr = [1, "1"];
let t = [1, "a"];
;
let size = 1;
console.log(size);
function calculateTax(income) {
    if (income > 30000) {
        return income * 0.3;
    }
    return 0;
}
let obj = {
    id: 1,
    name: 'xx',
    retire: (date) => {
        if (date)
            return false;
        return true;
    }
};
let empobj = {
    id: 1,
    name: 'xx',
    retire: (date) => {
        if (date)
            return false;
        return true;
    }
};
let h = {
    zym: () => { },
    work: () => { }
};
let a = 5;
let b = 2;
function add(x, y) {
    return y ? x + y : x;
}
let x = '2';
